---
import {Image} from "astro:assets";

interface Props {
	src: string;
	alt?: string;
	width?: number;
	height?: number;
}

const {src, alt = "", width = 700, height = 700} = Astro.props;
---

<Image src={src} alt={alt} width={width} height={height} loading="lazy" />
